#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: character to be checked
 * Return: 1 for uppercase, 0 otherwise
 */
int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
	{
		_putchar(i);
	}
	_putchar('\n');
	return (1);
}
