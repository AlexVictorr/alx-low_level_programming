#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers.
 * @a: first computed integer.
 * @b: second computed integer.
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	return (a * b);
}
